# Prompt de retomada — próxima sessão

> Escrito em **2026-09-12**, às 15h, a pedido do autor ("salva o contexto atual
> da conversa de forma otimizada"). Cole o bloco abaixo como primeira mensagem
> da próxima sessão.
>
> Ele é **curto de propósito**: a sessão nova não precisa de contexto colado,
> precisa de **rota e de regra**. O contexto está em `docs/`, que é
> auto-suficiente (`docs/README.md`, "as três árvores"). O que este prompt
> carrega além da rota são as **premissas que envelhecem sem commit**: o que
> está plugado na mesa, o que está instalado na máquina, o que o autor decidiu
> em voz.
>
> Sucede o [`RETOMADA_2026-09-11.md`](RETOMADA_2026-09-11.md), que fica como
> registro — e que ficou velho em um dia: dezenove commits depois, o item
> bloqueante dele ("a IDE do checkout não abre") fechou na primeira hora.

---

```text
Leia docs/ e nada mais. Entrada: docs/README.md. A FILA VIVA e'
docs/roadmaps/40-estado-e-continuidade.md §4 — comece por ela. A ORDEM da
fila e' a do docs/roadmaps/42-trilha-profunda-embarcados.md (§4: P0, P1, P2,
P4-MCU, P3, P5, P6, P7), com o §8 (o "efeito JetBrains") como CRITERIO DE
PRONTO de cada pilar e o §9 (a trilha Python completa) como o fim da linha.
Se for nova no projeto: docs/LEITURA_TECNICA.md.

REGRA ZERO, antes de aceitar qualquer item como pendente: MEDIR. Fila e'
hipotese. `ls` no artefato, `git log` na area, grep no gate, rodar. Em
2026-09-12 a medicao achou uma CDB de oito dias que o diagnostico dizia
"em dia" — e isso virou codigo no mesmo gesto (40 §7.18).

ONDE PAROU (2026-09-12, 15h): pilar 0 do 42 com CINCO fatias entregues —
  dominio `project` (9 frameworks por evidencia, SDKs, artefatos, alvo), a
  receita e as particoes do ESP-IDF LIDAS, build.size pela particao app, o
  INDICE do projeto INTEIRO (`index`: pastas, arquivos, declaracoes de
  C/C++/Rust; Python contado; `#nome` sem LSP) e o CONTEXTO DE COMPILADOR
  por arquivo (`index.context`: unidade da CDB, alvo do cargo, interpretador
  Python; a CDB envelhecida por CMakeLists.txt de subpasta). Protocolo
  0.95.0; 719 testes Rust; 34 harnesses; 143 metodos; 45 eventos.

O PROXIMO, na ordem (40 §4; 42 §3 P0 e §8):
  P0 que falta   o PRESET no configure automatico (o automatico JA' EXISTE na
                 UI: ProjectHealthController pede cmake.configure quando nao
                 ha' cache — medido em 2026-09-12 DEPOIS de o 42 §8 dizer o
                 contrario; corrigido); a CDB a partir do
                 file-api codemodel-v2 compileGroups quando o gerador nao
                 exporta; Bear como processo para Makefile; o alvo do kit para
                 o rust-analyzer; watch recursivo (o incremento so' segue as
                 pastas abertas); modelo por alvo/preset; map file; gramatica
                 Python para os simbolos; `rustup target list --installed`
  P1 ambiente    o dominio `setup` com TODAS as entradas de embarcado e Python
                 por distro; toolchain manager que LE a pasta do sysroot e
                 gera o toolchain file; importar kit Yocto (environment-setup)
                 e Buildroot; ".venv com uv" de um clique; E2 do 38 (permissao
                 por canal: dialout, uaccess, ModemManager)
  P2 ciclo MCU   E5 identidade Espressif (esptool chip-id/flash-id na porta),
                 E4 GRAVAR como configuracao de execucao (esptool com
                 flasher_args.json; probe-rs; picotool; dfu-util); a sonda
                 escolhida na lista e o OpenOCD deduzido do VID:PID
  P4 MicroPython o ESP32 da mesa com o firmware oficial gravado e o REPL do
                 mpremote na aba (1.29.0 instalado aqui)
  P3 depuracao   o painel SVD com ESCRITA: svd-parser (0.14.10, MIT/Apache) +
                 DAP readMemory/writeMemory (o gdb 17.2 daqui implementa os
                 dois) pelo gdb -i dap; svdFile pelo probe-rs. RTT/defmt.
  P5, P6, P7     qualidade (clang-tidy no clangd, ruff LSP, a lampada do
                 Alt+Enter), Linux embarcado (SSH: deploy, gdbserver, debugpy;
                 sshd local no gate), Rust profundo
  banco          executar consulta e escrever (40 §4 item 27) — a trilha
                 Python (42 §9) termina nele

A MESA E A MAQUINA (premissas que envelhecem — MEDIR antes de usar):
  - ESP32 CLASSICO (ESP32-D0WD-V3, CP2102 10c4:ea60) em /dev/ttyUSB0,
    root:dialout, o autor no dialout, auto-reset DTR/RTS OK; o ModemManager
    sonda a porta ~4 s depois de plugar. So' ele na mesa; C3/C6 vem.
  - esptool 5.3.1, mpremote 1.29.0, platformio 6.1.19, poetry 2.4.1 (pipx);
    probe-rs 0.32.0; OpenOCD 0.12+dev; gdb/gdbserver 17.2 (sem xtensa);
    picocom, minicom, bear 3.1.6 instalados; tio, espflash, idf.py, uv,
    basedpyright AUSENTES. Python 3.14.7 (o 3.15.0 sai em 2026-10-01; nao
    existe 3.16).
  - NAO ha' Docker: `docker` e' o shim podman-docker; Podman 5.8.4 rootless.
    A forma JSON do Docker real NAO foi verificada.
  - qmllint estrito le o qmltypes de build/linux-clang-debug-strict: rebuilde
    essa arvore antes de scripts/verificar-qml.sh quando um .qml novo entrar.
  - o gate completo (~5,5 min) morre por "low on memory" em segundo plano;
    rode em PRIMEIRO plano com timeout de 10 min.

DECISOES DO AUTOR NESTA PASSADA (40 §5; 42 §0/§7/§8/§9) — nao reabra:
  - Python ENTRA como vertical nativa (reverte "adiado"); Pylance PROIBIDO
  - monitor serial = PROCESSO na aba (tio|picocom|minicom|espflash); MPL-2.0
    NAO admitida como crate (serialport/espflash barrados; ferramentas GPL
    rodam como processo); Gravar = configuracao de execucao;
    ordem E1 -> E3 -> E5 -> E4 -> E2
  - Docker/Podman e Grafana NATIVOS com icone no rail; Grafana embutido
    continua PROIBIDO (AGPL) — HTTP API
  - so' o ESP32 classico na mesa; Raspberry Pi OS como alvo Linux; Yocto/
    Buildroot so' reconhecimento; P0 primeiro; AVR/Arduino NAO
  - A IDE LE O PROJETO INTEIRO com contexto de compilador (o `index`)
  - o "efeito JetBrains" e' CRITERIO DE PRONTO: zero-config = DETECTAR + UM
    CLIQUE com o comando visivel, NUNCA download calado
  - SIMULACAO FISICA/MATEMATICA FORA DO FOCO: o `sim` e o `kinein-sim` ficam,
    o gate testa, nenhuma fatia nova. Reabrir e' do autor

O QUE NAO SE NEGOCIA (AGENTS.md + ARCHITECTURE.md):
  - regra que mora so' em .md apodrece calada. Se importa, vira gate — e todo
    gate nasce de falha SILENCIOSA e e' provado por MUTACAO. Mutacao que o
    compilador pega antes do teste NAO vale (em 2026-09-12 foram 24 + 9 que
    valeram, e varias que nao valeram ate' o teste ficar mais forte)
  - corte por RESPONSABILIDADE, nunca por tamanho (§4 regra 9). Rust 500
    linhas sem #[cfg(test)]; QML visual 300, Controller/Host/Main 400; a
    catraca so' manda OLHAR
  - mexer em LIMITE e' decisao do autor, registrada
  - afirmou comportamento de API? FONTE e VERSAO. Licenca se le no LICENSE
    do repositorio, nunca no palpite SPDX do GitHub
  - numero em documento sem data e' afirmacao sobre AGORA, e o gate confere
  - `unsafe` proibido, inclusive escrever variavel de ambiente: injete por
    parametro (o `Ferramentas` do index/context e' o exemplo do dia)
  - dominio novo = arquivo novo em CADA camada: protocolo -> core -> handler
    -> lib.rs -> core_client_<dom>.cpp -> <Dom>{Event,Request}Router.qml ->
    <Dom>Controller.qml -> painel -> tst_<dom>.qml -> exercitacao -> 03-ipc
    -> 02 -> 40 §7.N no MESMO commit
  - commit em portugues no estilo do log; antes: bash scripts/verificar.sh
    (21 passos fixos + 5 do modo completo em 2026-09-12; conte com
    grep -c '^passo ' scripts/verificar.sh)

FORA DE ESCOPO, decidido e registrado — nao reabra:
  IA na IDE · Pylance · MS Serial Monitor, cpptools, nRF Connect, Wokwi,
  Dependi (41 §4) · MPL como crate · symbolica em Rust · Grafana embutido ·
  senha em disco · EditorConfig · EditorController.qml congelado · AVR/
  Arduino · Jetson/CUDA · STM32CubeIDE/MX · J-Link Software · simulacao
  (fora do foco) · download de toolchain calado
```

---

## Se a sessão perguntar "e o que acabou de ser feito?"

**Dezenove commits em 2026-09-11/12**, do gate que voltou a abrir a IDE até
o contexto de compilador por arquivo. Em uma frase por fatia:

```text
2a5e277  a IDE do checkout ABRE de novo (objeto AOT obsoleto na arvore, nao
         no fonte) — e o 20o gate executa o binario que compilou
7cad14b  frente F, fatia 1 — o fio: a tela alcanca o que o core ja' tinha
e123134  frente F, fatia 2 — QEMU + `gdb -i dap`: o ciclo de embarcado
         provado SEM placa, no gate
2372e38  4.1 — o clangd enxerga o compilador cross do kit (--query-driver)
6fd6d90  4.2 — o tamanho do ELF depois do build; SSH remoto anotado
77e6972  o ESP32 chegou a mesa: a conectividade bare metal MEDIDA (38)
11b1ac0  E1 serial.list — a IDE ve a porta serial SEM abri-la (rustix
         access; sysfs; udev; ModemManager)
b7fb948  41 — o ecossistema aberto de embarcados e Python, linear, com
         licenca lida e o que NAO entra
3402869  container — Docker e Podman nativos (shim detectado; ps/images em
         JSON; logs/shell na aba; compose em job; icone no rail)
7b6316d  E3 — o monitor serial como processo na aba (papel serialMonitor;
         espflash monitor --monitor-baud, nao --baud)
f03cf46  42 — a trilha PROFUNDA: oito pilares, pronto por familia
f5e006a  42 §0/§7 — as tres decisoes: so' o ESP32; Raspberry Pi OS; P0
d58dec9  P0 fatia 1 — o MODELO do projeto (9 frameworks por evidencia)
32a2760  P0 fatia 2 — flasher_args.json e partitions.csv LIDOS (offsets em
         branco como o gen_esp32part.py)
db7d778  P0 fatia 3 — build.size pela particao `app`, nao pelo .ld
b96eec8  P0 fatia 4 — o projeto INTEIRO lido: `index` (job, Tree-sitter
         tags, dedup, `#nome` sem LSP, barra de status)
01f1d72  P0 fatia 5 — o CONTEXTO DE COMPILADOR por arquivo (0.95.0): CDB nas
         duas formas, cargo metadata, interpretador; a CDB envelhecida por
         SUBPASTA que o cdb::status nao via
3053165  42 §8/§9 — o efeito JetBrains como criterio de pronto; a trilha
         Python completa; simulacao fora do foco
(este)   sincronizacao de fim de dia e este prompt
```

**As lições que atravessam os dois dias, e valem para a próxima sessão:**

- **A fila mente com boa intenção.** O item bloqueante do prompt anterior
  tinha causa errada; a CDB "em dia" tinha oito dias. Medir primeiro é o que
  fecha item em uma hora em vez de reabrir em uma semana.
- **Processo é injetado.** `cargo`, `poetry`, `python --version`, `docker`,
  `udevadm`, `esptool` — tudo que roda entra por parâmetro (`Ferramentas`,
  `ToolDetector`, `Ambiente`). O teste passa `None` ou um script falso, e
  nada da máquina entra. Dois testes que escrevem e executam scripts precisam
  de um `Mutex` entre si (`ETXTBSY`: o filho herda o descritor entre o fork e
  o exec).
- **Mutação que sobrevive é pergunta, não fracasso.** "Sem diretório mais
  longo" sobreviveu até existir um alvo `src/bin/tool/main.rs` dentro de
  `src/`; "features sem ordem" sobreviveu até haver três fora de ordem. O
  teste ficou mais forte porque a mutação disse onde ele era fraco.
- **A licença se lê no arquivo.** MPL-2.0 barrou `serialport` e `espflash`
  como crate; GPL roda como processo; a data e a fonte ficam no documento.
