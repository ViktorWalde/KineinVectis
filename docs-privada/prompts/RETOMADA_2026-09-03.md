# Prompt de retomada — sessão de 2026-09-03

> Escrito em 2026-09-02, ao fechar a etapa 9 do `roadmaps/30`. Cole o bloco
> abaixo como primeira mensagem da próxima sessão.
>
> Ele é **curto de propósito**: a sessão nova não precisa de contexto colado,
> precisa de rota e de regra. O contexto está em `docs/`, que é auto-suficiente
> (`docs/README.md`, "as três árvores").

---

```text
Leia docs/ e nada mais. Entrada: docs/README.md. Se for nova no projeto:
docs/LEITURA_TECNICA.md e depois docs/arquitetura/04-boot-e-comunicacao.md.

REGRA ZERO: fila e' hipotese, nao estado. Antes de aceitar qualquer item como
pendente, MEÇA — `ls` no artefato, `git log` na area, `grep` no gate. Em
2026-08-30 essa regra derrubou duas entradas falsas de fila no mesmo dia.

ONDE O PROJETO ESTA (medido em 2026-09-02, confira):
  roadmaps/30 FECHADO — 9 etapas feitas, a 6 paga em parte
  433 testes Rust, protocolo 0.65.0, gate completo verde
  17 arquivos em debito: cat scripts/arquitetura-baseline.txt

A FILA E' docs/roadmaps/34-depois-do-mvp.md. Ela ja' esta medida e ordenada.

COMECE PELA ETAPA 11, E ELA E' UMA PERGUNTA, NAO CODIGO:
  `ui/qml/editor/EditorController.qml` esta em 791/400. Das 97 funcoes,
  64 sao delegacao de uma linha — o arquivo e' FACHADA, nao logica mistura-
  da. Quebra-lo custa ~180 pontos de chamada em 13 arquivos, 92 deles no
  `ShellWorkspaceHost.qml`, que esta em 576/400 e ficaria pior.
  As tres saidas estao em roadmaps/34 §3.2 com o custo de cada uma.
  ARCHITECTURE.md §4 regra 8: levantar um limite e' decisao explicita
  REGISTRADA do autor. Me pergunte antes de escolher.

DEPOIS DA RESPOSTA, siga a ordem linear do 34 §7. Se eu disser "estou no
Kinein", a etapa 12 (registro de saidas do dogfooding) fura a fila.

COMO ESTE PROJETO TRABALHA — nao negocie estas:
  - gate nasce so' de falha SILENCIOSA, e todo gate/sonda e' provado por
    MUTACAO. Teste que nao reprova nao e' rede.
  - corte por RESPONSABILIDADE, nunca por tamanho (§4 regra 9). Depois de
    extrair um dominio, `grep` do conceito no arquivo de origem tem que
    voltar VAZIO.
  - catraca disparou? Os suspeitos, nesta ordem: sua mudanca, a categoria,
    o arquivo. Atualize a baseline NO MESMO COMMIT:
    bash scripts/verificar-arquitetura.sh --atualizar-baseline
  - afirmou comportamento de API? Cite fonte e versao.
  - numero em documento sem data e' afirmacao sobre AGORA, e o gate confere.

FORA DE ESCOPO, decidido e registrado — nao reabra:
  IA na IDE (2026-07-17) · Python adiado (2026-08-30) · Pylance PROIBIDO
  (licenca) · Docker/banco sao NATIVOS, nao plugins · EditorConfig auditado
  com resultado NEGATIVO em 2026-07-16
```

---

## Se a sessão perguntar "e o que acabou de ser feito?"

Etapa 9 do `roadmaps/30`, em 2026-09-02: busca e substituição **multi-linha**.
A documentação completa do domínio está em
[`docs/arquitetura/33-busca-no-projeto.md`](../../docs/arquitetura/33-busca-no-projeto.md).

Em uma frase: a guarda que recusava `\n` saiu **porque a razão dela saiu** — a
busca passou a casar no conteúdo, não linha a linha, e o preview voltou a contar
o que a escrita vai fazer. Para caber, `SearchController.qml` (420/400) foi
cortado em dois donos que só dividiam o nome.
