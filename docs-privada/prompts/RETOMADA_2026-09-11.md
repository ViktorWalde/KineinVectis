# Prompt de retomada — próxima sessão

> Escrito em **2026-09-10**, ao fechar o dia. Cole o bloco abaixo como primeira
> mensagem da próxima sessão.
>
> Ele é **curto de propósito**: a sessão nova não precisa de contexto colado,
> precisa de **rota e de regra**. O contexto está em `docs/`, que é
> auto-suficiente (`docs/README.md`, "as três árvores").
>
> Sucede o [`RETOMADA_2026-09-03.md`](RETOMADA_2026-09-03.md), que fica como
> registro.

---

```text
Leia docs/ e nada mais. Entrada: docs/README.md. A FILA VIVA e'
docs/roadmaps/40-estado-e-continuidade.md — comece por ela (o PONTO_ATUAL.md
DEIXOU de ser a fila em 2026-09-10; ele e' registro do porque, e o cabecalho
dele diz isso). Se for nova no projeto: docs/LEITURA_TECNICA.md.

REGRA ZERO, antes de aceitar qualquer item como pendente: MEDIR. Fila e'
hipotese. `ls` no artefato, `git log` na area, grep no gate, rodar. Nesta
semana a medicao derrubou tres afirmacoes de documento e duas de fila.

A PRIMEIRA COISA, e ela bloqueia o resto:
  A IDE COMPILADA DO CHECKOUT NAO ABRE. SIGABRT com display real E offscreen,
  nos dois presets; o AppImage em dist/ abre. Nao e' regressao — o binario de
  2026-09-06 aborta igual. Isolado: QV4_FORCE_INTERPRETER=1 abre e
  QML_DISABLE_DISK_CACHE=1 abre, entao e' o QML compilado em AOT contra o Qt
  6.11.2 do sistema. Detalhe e reproducao em roadmaps/40 §4.
  Enquanto isso valer, "pente-fino" so' alcanca o AppImage, que e' outro Qt.

DEPOIS DELA, o que o autor deixou escolhido/aberto (roadmaps/40 §4 e §8):
  - a SAIDA DOS TESTES nao chega a tela. `event.test.output` e' emitido e
    ninguem escuta; o BuildPanel tem outputModel e o TestsPanel nao tem. Num
    teste que falha, a razao esta' na saida. E' a fatia mais barata da lista.
  - o 20o GATE proposto e NAO criado: evento que o core emite e nenhuma tela
    consome. Mesmo criterio do 19o, uma camada acima. A varredura de
    2026-09-10 achou cinco casos; ele os pegaria sozinho. DECISAO DO AUTOR.
  - simulacao: falta salvar um SISTEMA, o oraculo da forma vetorial (o dsolve
    sobre sistema NAO foi medido — medir antes), unidades na forma algebrica,
    a EDP (com o motor compilado como fatia PROPRIA antes dela) e a vista 3D.
  - banco: executar consulta, escrever, TLS do postgres.

ARQUITETURA — leia ANTES de propor qualquer reorganizacao:
  docs/arquitetura/35-crescer-sem-god-object.md. Ele mede o que esta SAUDAVEL
  (medianas boas, zero ciclos, rebuild de 2 s, layout ja' recomendado) e diz o
  que a literatura recomenda e este projeto deve RECUSAR: quebrar o core em
  crates "para compilar mais rapido" (medido: nao ha' dor) e o cargo-pup
  (exige nightly). O achado real: o split e' FISICO, nao logico — 15 dos 24
  handlers tocam 0 ou 1 campo do `Core`.

O QUE NAO SE NEGOCIA (AGENTS.md + ARCHITECTURE.md):
  - regra que mora so' em .md apodrece calada. Se a regra importa, ela vira
    gate — e todo gate nasce de falha SILENCIOSA e e' provado por MUTACAO.
    Teste que nao reprova nao e' rede.
  - corte por RESPONSABILIDADE, nunca por tamanho (§4 regra 9). A catraca e'
    detector de fumaca; quando dispara, os suspeitos sao, nesta ordem: sua
    mudanca, a categoria, o arquivo.
  - mexer em LIMITE e' decisao do autor, registrada. Em 2026-09-10 isso
    aconteceu uma vez (header que so' DECLARA), e o limite virou funcao do que
    o arquivo declara — nao um numero escolhido.
  - afirmou comportamento de API? Cite FONTE e VERSAO. Nunca deduza do nome.
  - numero em documento sem data e' afirmacao sobre AGORA, e o gate confere.
  - `unsafe` e' proibido, e isso alcanca o teste: escrever variavel de
    ambiente virou unsafe na edicao 2024. Injete por parametro.
  - antes de commitar: bash scripts/verificar.sh  (19 verificacoes)

FORA DE ESCOPO, decidido e registrado — nao reabra:
  IA na IDE (2026-07-17) · Python adiado · Pylance PROIBIDO (licenca) ·
  Docker/banco sao NATIVOS, nao plugins · EditorConfig auditado com resultado
  NEGATIVO (2026-07-16) · Grafana embutido PROIBIDO (AGPL) · senha em disco
  PROIBIDA · simbolica em Rust PROIBIDA (licenca) · EditorController.qml
  congelado ate' decisao do autor
```

---

## Se a sessão perguntar "e o que acabou de ser feito?"

**Treze commits em 2026-09-10.** O domínio `sim` inteiro saiu do limbo: ele
estava pronto e **não commitado** havia quatro dias, com o motor vetorial sem
tela.

Em uma frase por fatia:

```text
o motor          o dominio `sim` no core e no protocolo: catalogo de duas
                 camadas, ligacao EXPLICITA, integrador escalar e vetorial
                 verificados por ORDEM DE CONVERGENCIA
a tela           o ramo vetorial ligado ao motor — ele existia sem porta, e a
                 lista OFERECIA tres conceitos que a tela escalar aceitava e
                 respondia errado
o 19o gate       componente entregue que nenhuma tela abre. Nasceu do buraco
                 acima, e reproduz o achado quando mutado
o oraculo        a coluna `exato` respondia por OUTRA equacao — erro de 2,5e-2
                 numa integracao correta ate' 3,2e-7, setenta e oito mil vezes.
                 O conserto nao foi esconder o numero: foi dar PROCEDENCIA
as unidades      checadas nas formas que integram, na mesma ida do oraculo.
                 Tres armadilhas medidas antes do codigo, e as tres dariam
                 veredito errado EM SILENCIO
a catraca        o `core_client.h` saiu de 500/500 pela CATEGORIA, nao pelo
                 tamanho: o limite virou 2x o que o arquivo declara
a remediacao     o arquitetura/02 listava uma pasta `templates/` que NUNCA
                 existiu, e o PONTO_ATUAL dizia protocolo 0.62.0 contra 0.88.0
a varredura      cinco coisas que o core calcula e nenhuma tela mostra
a analise        docs/arquitetura/35, com o que a pesquisa recomenda e o que
                 este projeto deve recusar
```

**A lição que atravessa o dia, e vale para a próxima sessão:** *"o motor passa
nos testes"* e *"o usuário alcança o motor"* são afirmações diferentes. O
repositório só tinha rede para a primeira, ganhou uma para a segunda (o 19º
gate), e a varredura mostrou que falta o andar de cima — evento que o core emite
e nenhuma tela consome.
